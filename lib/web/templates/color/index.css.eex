<%= if @is_client do %>
/**
 * Web client colors
 */

body {
  background-color: #161624;
  color: <%= Config.color_text_color() %>;
}

.character-info {
  background-color: rgba(0,0,0,.4);
  color: <%= Config.color_character_info_text() %>;
}

.room-info {
  background-color: rgba(0,0,0,.4);
  color: <%= Config.color_room_info_text() %>;
}

.room-info .exits .white {
  color: <%= Config.color_room_info_exit() %>;
}

.experience-bar,
.stat-block {
  background-color: #000;
  border: 0px;
  border-right: 2px solid rgba(0,0,0,.4);
}

.panel {
  border-left: 6px solid rgba(0,0,0,.1);
}

#health .container {
  color: <%= Config.color_health_bar_background() %>;
  border-bottom: 4px solid rgba(0,0,0,.4);
  border-right: 2px solid rgba(0,0,0,.4);
}
#health .percentage {
  background-image: linear-gradient(to right, #c60303 , #f96900);
}

#skills .container {
  color: <%= Config.color_skill_bar_background() %>;
  border-bottom: 4px solid rgba(0,0,0,.4);
  border-right: 2px solid rgba(0,0,0,.4);
}
#skills .percentage {
  background-image: linear-gradient(to right, #175ee8 , #322684);
}

#endurance .container {
  color: <%= Config.color_endurance_bar_background() %>;
  border-bottom: 4px solid rgba(0,0,0,.4);
  border-right: 2px solid rgba(0,0,0,.4);
}
#endurance .percentage {
  background-image: linear-gradient(to right, #34c400 , #0b6209);
}

#experience .percentage {
  background: <%= Config.color_experience_bar() %>;
}

.prompt input[type='text'] {
  background-color: #000;
  color: #FFF;
}

.tooltip {
  color: white;
}

.tooltip:hover:after,
.command:hover:after {
  background: #657b83;
}

.target-bar .button,
.action-bar .button {
  color: white;
  border: 2px solid #171b24;
  background-color: #171b24;
}

.action-bar .container {
  background-color: #b58900;
}

.action-bar .container:hover {
  background-color: #C2960D;
}

.action-bar .inactive .container {
  background-color: #825600;
}

.action-bar .container.empty {
  background-color: #161d20;
}

.target-bar .button {
  border: 2px solid #5e42c1;
  background-color: #fdf6e3;
}

.target-bar .container {
  background-color: #293159;
}

.target-bar .container:hover {
  background-color: #3e4fa8;
}

.target-bar .button.selected {
  background-color: #293159;
  border: 2px solid #5e42c1;
}
<% end %>

<%= if @is_home do %>
/**
 * Home page colors
 */

.site-header {
  background-color: <%= Config.color_home_header() %>;
}

a {
  color: <%= Config.color_home_link() %>;
}

a:hover {
  color: <%= Config.color_home_link_hover() %>;
}

.btn-primary {
  background-color: <%= Config.color_home_primary() %>;
  border-color: <%= Config.color_home_primary() %>;
}

.btn-primary:hover {
  background-color: <%= Config.color_home_primary_hover() %>;
  border-color: <%= Config.color_home_primary_hover() %>;
}

.btn-secondary {
  background-color: <%= Config.color_home_secondary() %>;
  border-color: <%= Config.color_home_secondary() %>;
  color: <%= Config.color_home_secondary_text() %> !important;
}

.btn-secondary:hover {
  background-color: <%= Config.color_home_secondary_hover() %>;
  border-color: <%= Config.color_home_secondary_hover() %>;
  color: <%= Config.color_home_secondary_text() %> !important;
}
<% end %>

/**
 * Base Colors
 */

.color-code-black,
.black {
  color: <%= Config.color_black() %>;
}

.color-code-red,
.red {
  color: <%= Config.color_red() %>;
}

.color-code-green,
.green {
  color: <%= Config.color_green() %>;
}

.color-code-yellow,
.yellow {
  color: <%= Config.color_yellow() %>;
}

.color-code-blue,
.blue {
  color: <%= Config.color_blue() %>;
}

.color-code-magenta,
.magenta {
  color: <%= Config.color_magenta() %>;
}

.color-code-cyan,
.cyan {
  color: <%= Config.color_cyan() %>;
}

.color-code-white,
.white {
  color: <%= Config.color_white() %>;
}

.color-code-brown,
.brown {
  color: <%= Config.color_brown() %>;
}

.color-code-dark-green,
.dark-green {
  color: <%= Config.color_dark_green() %>;
}

.color-code-grey,
.grey {
  color: <%= Config.color_grey() %>;
}

.color-code-light-grey,
.light-grey {
  color: <%= Config.color_light_grey() %>;
}

/**
 * Custom colors
 */

<%= Enum.map(@color_codes, fn color_code -> %>
.color-code-<%= color_code.key %> {
  color: <%= color_code.hex_code %>;
}
<% end) %>

/**
 * Home page, admin
 */
.announcement pre,
.terminal,
.console {
  background-color: <%= Config.color_background() %>;
  color: <%= Config.color_text_color() %>;
}


/* width */
body::-webkit-scrollbar {
  width: 3px;
}

/* Track */
body::-webkit-scrollbar-track {
  background: rgba(0,0,0,.6);
}

/* Handle */
body::-webkit-scrollbar-thumb {
  background: rgba(0,0,0,.8);
}

/* Handle on hover */
body::-webkit-scrollbar-thumb:hover {
  background: rgba(0,0,0,.4);
}

/* width */
.panel::-webkit-scrollbar {
  width: 3px;
}

/* Track */
.panel::-webkit-scrollbar-track {
  background: rgba(0,0,0,.6);
}

/* Handle */
.panel::-webkit-scrollbar-thumb {
  background: rgba(0,0,0,.8);
}

/* Handle on hover */
.panel::-webkit-scrollbar-thumb:hover {
  background: rgba(0,0,0,.4);
}
